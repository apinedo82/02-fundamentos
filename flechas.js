
function sumar(a,b){
    return a + b;
}  // funcion tradicional

const sumar = (a, b) => {
    return a + b
}

const sumar1 = (a, b) => a + b; // si la funcion flecha tiene una linea se puede hacer asi


console.log( sumar1(5,10));