const nombre = 'Deedpool';
const real = 'Wade winston';

const normal = nombre + ' ' + real;
const template = `${nombre } ${real}`; // ejemplo template string

console.log(normal);
console.log(template);

const parrafo = `
<h1>Hola</h1>
<p>Mundo</p>
`;

console.log(parrafo);